package com.example.testkinoapp.repository

object Endpoints {
    object Default {
        const val apiV3: String = "/v3.0"
        const val draws: String = "/draws"
        const val upcoming: String = "/upcoming"
        const val drawDate: String = "/draw-date"

    }
}